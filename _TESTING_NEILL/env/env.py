import logging

logging.basicConfig(
    # filename='neill.log',
    level=logging.DEBUG,
    format='LUMA - %(asctime)s - %(levelname)s :: %(message)s'
)

logging.info('Setting up main environment.')

